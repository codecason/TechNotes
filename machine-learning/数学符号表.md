**常用 LaTeX 数学符号表**。

## 1. 基本运算符

| 显示      | LaTeX     | 含义            |
| --------- | --------- | --------------- |
| (+)       | `+`       | 加              |
| (-)       | `-`       | 减              |
| (\times)  | `\times`  | 乘              |
| (\cdot)   | `\cdot`   | 点乘 / 乘       |
| (\div)    | `\div`    | 除              |
| (\pm)     | `\pm`     | 正负            |
| (\mp)     | `\mp`     | 负正            |
| (\ast)    | `\ast`    | 星号运算        |
| (\star)   | `\star`   | 星号            |
| (\circ)   | `\circ`   | 复合 / 圆圈运算 |
| (\bullet) | `\bullet` | 点              |

## 2. 比较与关系符号

| 显示      | LaTeX     | 含义        |
| --------- | --------- | ----------- |
| (=)       | `=`       | 等于        |
| (\neq)    | `\neq`    | 不等于      |
| (>)       | `>`       | 大于        |
| (<)       | `<`       | 小于        |
| (\geq)    | `\geq`    | 大于等于    |
| (\leq)    | `\leq`    | 小于等于    |
| (\gg)     | `\gg`     | 远大于      |
| (\ll)     | `\ll`     | 远小于      |
| (\approx) | `\approx` | 约等于      |
| (\sim)    | `\sim`    | 相似 / 渐近 |
| (\simeq)  | `\simeq`  | 近似等价    |
| (\equiv)  | `\equiv`  | 恒等于      |
| (\propto) | `\propto` | 正比于      |

## 3. 集合符号

| 显示          | LaTeX         | 含义     |
| ------------- | ------------- | -------- |
| (\in)         | `\in`         | 属于     |
| (\notin)      | `\notin`      | 不属于   |
| (\subset)     | `\subset`     | 真子集   |
| (\subseteq)   | `\subseteq`   | 子集     |
| (\supset)     | `\supset`     | 真超集   |
| (\supseteq)   | `\supseteq`   | 超集     |
| (\cup)        | `\cup`        | 并集     |
| (\cap)        | `\cap`        | 交集     |
| (\setminus)   | `\setminus`   | 差集     |
| (\varnothing) | `\varnothing` | 空集     |
| (\mathbb{N})  | `\mathbb{N}`  | 自然数集 |
| (\mathbb{Z})  | `\mathbb{Z}`  | 整数集   |
| (\mathbb{Q})  | `\mathbb{Q}`  | 有理数集 |
| (\mathbb{R})  | `\mathbb{R}`  | 实数集   |
| (\mathbb{C})  | `\mathbb{C}`  | 复数集   |

## 4. 逻辑符号

| 显示              | LaTeX             | 含义            |
| ----------------- | ----------------- | --------------- |
| (\forall)         | `\forall`         | 任意 / 对所有   |
| (\exists)         | `\exists`         | 存在            |
| (\nexists)        | `\nexists`        | 不存在          |
| (\land)           | `\land`           | 且              |
| (\lor)            | `\lor`            | 或              |
| (\neg)            | `\neg`            | 非              |
| (\Rightarrow)     | `\Rightarrow`     | 推出            |
| (\Leftarrow)      | `\Leftarrow`      | 由……推出        |
| (\Leftrightarrow) | `\Leftrightarrow` | 等价 / 当且仅当 |
| (\therefore)      | `\therefore`      | 因此            |
| (\because)        | `\because`        | 因为            |

## 5. 箭头符号

| 显示              | LaTeX             | 含义     |
| ----------------- | ----------------- | -------- |
| (\to)             | `\to`             | 到       |
| (\leftarrow)      | `\leftarrow`      | 左箭头   |
| (\rightarrow)     | `\rightarrow`     | 右箭头   |
| (\leftrightarrow) | `\leftrightarrow` | 双向箭头 |
| (\mapsto)         | `\mapsto`         | 映射到   |
| (\Rightarrow)     | `\Rightarrow`     | 推出     |
| (\Longrightarrow) | `\Longrightarrow` | 长推出   |
| (\Leftrightarrow) | `\Leftrightarrow` | 等价     |
| (\uparrow)        | `\uparrow`        | 向上     |
| (\downarrow)      | `\downarrow`      | 向下     |

## 6. 微积分与代数符号

| 显示            | LaTeX           | 含义     |
| --------------- | --------------- | -------- |
| (\sum)          | `\sum`          | 求和     |
| (\prod)         | `\prod`         | 连乘     |
| (\int)          | `\int`          | 积分     |
| (\iint)         | `\iint`         | 二重积分 |
| (\iiint)        | `\iiint`        | 三重积分 |
| (\oint)         | `\oint`         | 曲线积分 |
| (\lim)          | `\lim`          | 极限     |
| (\frac{dy}{dx}) | `\frac{dy}{dx}` | 导数     |
| (\partial)      | `\partial`      | 偏导     |
| (\nabla)        | `\nabla`        | 梯度算子 |
| (\infty)        | `\infty`        | 无穷     |
| (\sqrt{x})      | `\sqrt{x}`      | 平方根   |
| (\sqrt[n]{x})   | `\sqrt[n]{x}`   | n 次根   |

常见写法：

```latex
\sum_{i=1}^{n} i
```

显示为：
$$
\sum_{i=1}^{n} i
$$

```latex
\int_{a}^{b} f(x)\,dx
```

显示为：
$$
\int_{a}^{b} f(x),dx
$$

## 7. 希腊字母

| 小写       | LaTeX      | 大写      | LaTeX     |
| ---------- | ---------- | --------- | --------- |
| (\alpha)   | `\alpha`   | (A)       | `A`       |
| (\beta)    | `\beta`    | (B)       | `B`       |
| (\gamma)   | `\gamma`   | (\Gamma)  | `\Gamma`  |
| (\delta)   | `\delta`   | (\Delta)  | `\Delta`  |
| (\epsilon) | `\epsilon` | (E)       | `E`       |
| (\zeta)    | `\zeta`    | (Z)       | `Z`       |
| (\eta)     | `\eta`     | (H)       | `H`       |
| (\theta)   | `\theta`   | (\Theta)  | `\Theta`  |
| (\lambda)  | `\lambda`  | (\Lambda) | `\Lambda` |
| (\mu)      | `\mu`      | (M)       | `M`       |
| (\pi)      | `\pi`      | (\Pi)     | `\Pi`     |
| (\rho)     | `\rho`     | (P)       | `P`       |
| (\sigma)   | `\sigma`   | (\Sigma)  | `\Sigma`  |
| (\phi)     | `\phi`     | (\Phi)    | `\Phi`    |
| (\omega)   | `\omega`   | (\Omega)  | `\Omega`  |

常见变体：

| 显示          | LaTeX         |
| ------------- | ------------- |
| (\varepsilon) | `\varepsilon` |
| (\vartheta)   | `\vartheta`   |
| (\varphi)     | `\varphi`     |
| (\varpi)      | `\varpi`      |
| (\varrho)     | `\varrho`     |

## 8. 上标、下标、分式

| 显示           | LaTeX          |
| -------------- | -------------- |
| (x^2)          | `x^2`          |
| (x^{n+1})      | `x^{n+1}`      |
| (a_i)          | `a_i`          |
| (a_{ij})       | `a_{ij}`       |
| (\frac{a}{b})  | `\frac{a}{b}`  |
| (\dfrac{a}{b}) | `\dfrac{a}{b}` |
| (\binom{n}{k}) | `\binom{n}{k}` |

## 9. 括号与定界符

| 显示                       | LaTeX                      |
| -------------------------- | -------------------------- |
| ((x))                      | `(x)`                      |
| ([x])                      | `[x]`                      |
| ({x})                      | `\{x\}`                    |
| (\langle x\rangle)         | `\langle x\rangle`         |
| (                          | x                          |
| (\|x\|)                    | `|x|`                      |
| (\left(\frac{a}{b}\right)) | `\left(\frac{a}{b}\right)` |

## 10. 常用字体

| 显示           | LaTeX          | 用途     |
| -------------- | -------------- | -------- |
| (\mathbb{R})   | `\mathbb{R}`   | 数集     |
| (\mathcal{F})  | `\mathcal{F}`  | 花体字母 |
| (\mathbf{x})   | `\mathbf{x}`   | 粗体     |
| (\mathrm{d}x)  | `\mathrm{d}x`  | 正体文字 |
| (\mathit{ABC}) | `\mathit{ABC}` | 斜体     |
| (\mathsf{ABC}) | `\mathsf{ABC}` | 无衬线   |
| (\mathtt{ABC}) | `\mathtt{ABC}` | 等宽字体 |

## 11. 常用函数

| 显示         | LaTeX        |
| ------------ | ------------ |
| (\sin x)     | `\sin x`     |
| (\cos x)     | `\cos x`     |
| (\tan x)     | `\tan x`     |
| (\log x)     | `\log x`     |
| (\ln x)      | `\ln x`      |
| (\exp x)     | `\exp x`     |
| (\max x)     | `\max x`     |
| (\min x)     | `\min x`     |
| (\arg\max x) | `\arg\max x` |
| (\arg\min x) | `\arg\min x` |

## 12. 矩阵写法

```latex
\begin{pmatrix}
a & b \\
c & d
\end{pmatrix}
```

显示为：
$$
\begin{pmatrix}
a & b \\
c & d
\end{pmatrix}
$$
常见矩阵括号：

| 显示                                  | LaTeX 环境 |
| ------------------------------------- | ---------- |
| (\begin{pmatrix}a&b\c&d\end{pmatrix}) | `pmatrix`  |
| (\begin{bmatrix}a&b\c&d\end{bmatrix}) | `bmatrix`  |
| (\begin{Bmatrix}a&b\c&d\end{Bmatrix}) | `Bmatrix`  |
| (\begin{vmatrix}a&b\c&d\end{vmatrix}) | `vmatrix`  |
| (\begin{Vmatrix}a&b\c&d\end{Vmatrix}) | `Vmatrix`  |